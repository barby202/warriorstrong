pipeline {
    agent { 
        label 'node1 || node2 || node3 || node4 || node5'
         }
    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
    }
}
